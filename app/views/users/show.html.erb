<h1>Your Reservations/Dashboard</h1>

<p>To create a new reservation, <%= link_to "view all restaurants!", restaurants_path %></p>
<%# linking the choose a resturant to index page for all restaurants %>

<p> Hi <%= @user.name %>! You have <%= @user.reservations.count %> reservation(s)! Yum yum! </p>

  <% @user.reservations.each do |reservation| %>
  <div class="user_reservation">
      <h3><%= reservation.restaurant.name %> </h3>
      <div>Time: <%= reservation.datetime %> </div>
      <div>Location: <%= reservation.restaurant.address %></div>
      <div>Party Size: <%= reservation.seats %> </div>
      <p>To edit reservation: <%= link_to "Edit", edit_restaurant_reservation_path(@user.id) %></p>
  </div>
<% end %>





<!-- method: :delete, data: { confirm: "Are you sure you want to delete this?" } %> -->

<hr>


<h4> Your restaurants: (<%= @user.restaurants.count %>) </h4>

<ul>
<% @owned_restaurants.each do |restaurant| %>

    <li>Restaurant name: <%= restaurant.name %></br>
    Total capacity: <%= restaurant.capacity %>
    <p><%= link_to "Update this restaurant", edit_restaurant_path %><br />
    <%= link_to "View reservations", restaurant_reservations_url(:restaurant_id => restaurant.id) %></p>

<% end  %>
</ul>
<hr>
